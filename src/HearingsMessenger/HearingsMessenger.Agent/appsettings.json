{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    },
    "EventLog": {
      "LogLevel": {
        "Default": "Information"
      }
    }
  },
  "Agent": {
    "Port": 7443,
    "//CertPath": "Path to a PFX whose SAN matches this host's FQDN. Empty = ASP.NET dev cert (local testing only).",
    "CertPath": "",
    "//CertPassword": "For production, supply via user-secrets or an environment variable, NOT this file.",
    "CertPassword": ""
  }
}
